# Node Cluster Example

`server.ts` demonstrates the Node clustered adapter and graceful worker
shutdown. It is a Node-only deployment example; use `runtime-node-cluster` and
configure the port through the application entrypoint. Cluster behavior is
covered by the Node integration suite.
